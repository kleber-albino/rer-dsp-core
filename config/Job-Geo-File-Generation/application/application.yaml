spring:
  application:
    name: dsp-geo-file-generation
  main:
    web-application-type: none
  datasource:
    batch:
      url: jdbc:postgresql://dsp-db:5432/dsp-db?currentSchema=geo_file_generation
      username: dsp
      password: dsp
      driver-class-name: org.postgresql.Driver
      hikari:
        maximum-pool-size: 5
        minimum-idle: 1
        pool-name: batch-pool
        connection-test-query: SELECT 1
    target:
      url: jdbc:postgresql://dsp-db:5432/dsp-db
      username: dsp
      password: dsp
      driver-class-name: org.postgresql.Driver
      hikari:
        maximum-pool-size: 5
        minimum-idle: 1
        pool-name: target-pool
    geo-target:
      url: jdbc:postgresql://dsp-geoserver-db:5432/dsp-geoserver-db
      username: dsp_geo
      password: dsp_geo
      driver-class-name: org.postgresql.Driver
      hikari:
        maximum-pool-size: 10
        minimum-idle: 2
        pool-name: geo-target-pool
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: never
dsp:
  download:
    themes-file: file:/config/downloadThemesConfig.json
  object-storage:
    endpoint: ''
    region: us-east-1
    bucket: dsp-geo-files
    access-key: ''
    secret-key: ''
    path-style-access: true
geo-file-generation:
  chunk-size: 1
  orphan-cleanup-enabled: true
execution-jobs:
  geo-file-generation-job: false
logging:
  level:
    br.car.dsp_geo_file: INFO
